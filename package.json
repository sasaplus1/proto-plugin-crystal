{
	"devDependencies": {
		"@biomejs/biome": "2.3.11",
		"@extism/extism": "2.0.0-rc13"
	},
	"engines": {
		"node": "24",
		"npm": "11"
	},
	"private": true,
	"scripts": {
		"build": "cd moonbit && moon build --target wasm --release && cp _build/wasm/release/build/main/main.wasm ../crystal.wasm",
		"check": "biome check --write",
		"test": "node --test index.test.cjs"
	}
}
